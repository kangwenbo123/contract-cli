# AI 变更记录：CLI MCP 审批支持

## 2026-09-14（审批附件必须具有上传凭据）
- 改动内容：删除审批附件依赖已有业务关系放行的路径，要求同用户有效 commit 凭据；同步接口说明与 CLI Skill。
- 影响范围：MCP 审批附件来源校验；评论复用及文件下载规则保持不变。
- 验证结果：JDK 11 隔离编译、Surefire 定向测试，旧实现 1 项断言失败、修改后 6/6 通过；CLI 相关 Go 测试通过。
- 未处理项或风险项：完整 Maven 编译受既有 TaskQueryItemVO 依赖方法缺失阻塞；未部署、未线上验证、未提交。

## 2026-09-14（全部合同文件下载 Skill）

- 改动内容：补齐合同文件分类、表单文件、多个流程审批及递归评论附件的收集、去重和逐个下载流程。
- 影响范围：仅 contract-cli-contract Skill 及引用资料，复用现有 user 查询和单文件下载命令。
- 关键决策：区分单值/多值流程 ID；完整路径保存、同名消歧、部分失败继续，未知清单不报告全部完成。
- 验证结果：核对后端响应 DTO，Skill 格式校验、相关 Go 回归测试和 git diff --check 通过。
- 未处理项或风险项：未执行本轮线上全流程下载；历史流程附件仍以服务端归属校验为准；未提交。

## 2026-09-08（整理记录位置）

- 改动内容：移除主工作区和旧网关临时工作区 docs/ai-changes.md 中本任务追加的记录，保留各自原有历史。
- 关键决策：遵循用户最新要求，后续仅写需求专属记录，不再写通用 ai-changes.md。
- 验证结果：两个通用文件均与各自 HEAD 完全一致；本需求专属记录和测试报告保留。
- 影响范围：仅记录整理，未修改业务代码、未提交。

## 2026-09-08（发版后异常再次复验）

- 改动内容：重试合同详情及两类评论边界，将最新证据补到飞书报告开头并回读确认。
- 验证结果：同合同 3 次 30 秒未返回，另一合同 20 秒未返回；对照流程详情成功；空评论与无效父评论仍成功返回，后者无法回读。
- 影响范围：新增 2 次评论成功响应，未修改业务代码。
- 未处理项或风险项：未证明与发布事件的因果关系，三个问题仍待处理；未提交。

## 2026-09-08（dev 全场景复测与飞书报告）

- 改动内容：重新实操 dev，创建独立本轮报告并归档飞书 CLM 节点，文档 owner 已转给当前用户。
- 影响范围：53 条检查记录；3 份新附件、5 次成功返回的评论写入、2 个审批任务状态变更，未回滚。
- 验证结果：28 条正常成功、21 条预期拒绝；4 条偏离预期，涉及合同详情两次超时、空评论及无效父评论。
- 关键决策：实际等待 310 秒验证下载地址过期 403；区别跨合同校验与未覆盖的跨用户权限，未将旧结果拼入本轮结论。
- 文档：[本轮 dev 实操报告](https://ysi13ckdb9.feishu.cn/wiki/R2USwkIVkiPg3NkX7DhcgL04nUe)，已回读核对 53 个场景和异常章节。
- 未处理项或风险项：三个待处理问题保留，30 分钟上传授权过期/第二用户/app 未实测；本轮未修改业务代码、未提交。

## 2026-09-08（dev 预签名下载复测通过）

- 改动内容：更新测试报告，补充新部署 TOS 下载的真实证据。
- 影响范围：仅报告与 AI 记录；未修改业务代码或业务数据。
- 关键决策：正文使用合同详情的当前文件 ID，附件按上传原件做字节/哈希比对。
- 验证结果：正文 30208 字节、评论/审批附件各 623 字节均下载成功；两附件 SHA-256 与原件一致。
- 未处理项或风险项：六接口分轮正向功能均通过；过期签名和跨用户权限等负向实测未补测，未提交。

## 2026-09-08（对齐 TOS 预签名下载）

- 改动内容：依据 CLM 24cfab505f 更新协议文档、Skill、下载注释与签名 URL 回归用例，撤回旧网关配置草案。
- 影响范围：CLI 下载说明和验证；直接下载逻辑已兼容新协议，无需增加行为分支。
- 关键决策：原样消费 TOS HTTPS 签名 URL，不附加用户 Token 或重写签名查询串；保留完整性校验和旧文件保护。
- 验证结果：针对性下载测试通过；dev 元数据仍返回旧 ticket 地址，在线新协议尚未验证。
- 未处理项或风险项：待确认最新后端部署环境；旧网关方案未部署且已撤回，未提交代码。

## 2026-09-08（dev 审批动作实测）

- 改动内容：补充带新附件的审批通过/拒绝与 done/notice 查询实测报告。
- 影响范围：两条 dev 审批任务实际改变状态、新上传并关联两份测试附件；仅报告/AI 记录修改。
- 关键决策：通过后进入盖章节点；拒绝后退回提交节点，两个流程均仍 RUNNING。
- 验证结果：四类操作均 code=0，审批详情回读确认任务完成、附件关联和后继节点；累计六接口 5 通过、1 下载失败。
- 未处理项或风险项：下载网关修复尚未部署；测试数据保留，无回滚，无代码提交。

## 2026-09-08（修复人员 ID 查询参数并复测）

- 改动内容：两个 user 查询 ToolSpec 固定 user_id_type=user_id，更新接口/Skill 与测试报告。
- 影响范围：流程详情、个人任务列表；app 分支和评论/审批动作协议保持原状。
- 关键决策：复用 FixedQuery，不恢复隐式 CommonQuery 或扩大其他接口参数范围。
- 验证结果：命令回归先红后绿，make test 通过；新 CLI 的流程详情和 page_size=10 待办查询均 code=0。
- 未处理项或风险项：dev 下载仍 401；网关配置在独立工作区待 Helm 校验和部署；审批动作未执行，未提交。

## 2026-09-08（实测失败跨服务定位）

- 改动内容：补充测试报告的跨服务根因、HTTP 对照结果与证据边界。
- 影响范围：仅测试报告与 AI 记录；未改业务代码、在线配置或审批状态。
- 关键决策：人员 ID 默认 open_id 与组织服务枚举不兼容；下载 ticket 路径鉴权放行存在配置缺口，在线配置仍待核实。
- 验证结果：原 CLI 两接口复现 110000；同请求显式 user_id_type=user_id 均成功；新 ticket 无 Token 下载仍 401。
- 未处理项或风险项：CLI/网关修复未实施，历史超时未单独归因；未提交、未推送。

## 2026-09-08（dev 真实接口实测报告）

- 改动内容：新增六个 User MCP 业务接口的实测报告，区分通过、失败与受阻未测，记录测试数据和复现入口。
- 影响范围：仅报告与 AI 记录；使用 dev 中间分支临时 worktree 加本轮调整构建，6 个核心源码文件与开发工作区一致。
- 验证结果：评论列表、带附件评论创建及两类附件三步上传实测通过；流程详情/任务列表失败，下载第二段 HTTP 401；审批通过/拒绝未执行。
- 关键决策：任务 ID 与流程 ID 分开使用；评论使用新上传并 commit 的文件；不把自动化测试结果当作真实后端验收结果。
- 未处理项或风险项：六接口尚未全部通过；dev 留有 3 个测试上传文件和 1 条评论；网关/后端根因、审批动作、权限过期等仍待验证；未提交、未推送。

## 2026-09-07（对齐后端新附件授权与 ticket 下载）

- 改动内容：user 的 `reviewAttachment` / `approveAttachment` 自动执行 prepare → content → commit，仅确认提交后输出文件 ID；下载优先按实际 `Content-Length` 校验，缺失时回退元数据。
- 影响范围：CLI 上传/下载、合同 service 流式 multipart、帮助、接口说明、合同 Skill 与回归测试；app 上传和其他 user 类型沿用原接口。
- 关键决策：复用原流式 reader；临时上传不带 Token/Cookie、不跟随重定向、不暴露会话；业务失败立即停止，未知写结果保留不确定性及取消原因；文档明确同用户 30 分钟授权、已有文件复用、字符串 ID 和部分成功。
- 验证结果：TDD 先复现旧上传路径、历史大小误判及异常分类问题再修复；`make test`、CLI/contract/openplatform race、`go vet ./...`、gofmt 和差异检查通过；本地 qfei-code-review 1.8.4 未发现遗留问题。
- 未处理项或风险项：未进行真实后端的跨用户、授权过期及评论/审批串联测试；当前协议仅接受 POST 与空 upload_headers；修改留在 `codex/202609-task-release`，未提交、未推送。

## 2026-09-07（业务失败退出与审批意见脱敏）

- 改动内容：新增纯函数校验 MCP 响应业务状态，失败仍输出响应并返回错误；将 `--comment` 纳入现有参数脱敏。
- 影响范围：user 审批详情、评论、任务命令和共享日志参数名单，以及对应测试、接口说明、合同 Skill 与测试计划。
- 关键决策：仅接入本期 user 命令；app、其他既有 user 命令、下载专用校验、HTTP 与重试策略保持不变；错误摘要不含服务端正文。
- 验证结果：先补测试复现两个问题，再完成修复；目标回归、`make test`、相关 CLI/openplatform 模块 race、`go vet ./...`、gofmt 与差异检查通过；本地 qfei-code-review 未发现新增问题。
- 未处理项或风险项：未进行真实后端权限联调；未提交、未推送；业务失败的脚本现在需处理非零退出码。

## 2026-09-07（代码复审修复）

- 改动内容：拆分审批与文件下载职责，补齐覆盖成功测试，并为不支持硬链接的文件系统增加独占创建降级。
- 影响范围：`internal/cli` 的 user 文件下载实现、测试、帮助文档及合同 Skill。
- 验证结果：目标红绿回归、`go test ./...`、`go test -race ./...`、`go vet ./...`、`make test`、Linux/Windows amd64 构建、gofmt 与 `git diff --check` 均通过。
- 未处理项或风险项：跨平台不再承诺统一原子替换；进程被强制终止时的文件系统持久化语义仍由操作系统决定。

## 2026-09-07

- 改动内容：新增 6 个 User MCP 审批接口的开发、联调和 CLI 使用说明。
- 影响范围：`docs/user-mcp-approval-interfaces.md`、README 文档入口及文档契约测试。
- 验证结果：文档契约红绿测试、`make test`、`git diff --check`、接口字段自检和本地 qfei-code-review 通过。
- 未处理项或风险项：不修改接口实现、CLI 行为、飞书原始接口文档、版本或依赖。

## 2026-09-05

- 改动内容：为六个 user MCP 审批接口补齐评论、任务、详情和文件下载命令，保留原 app 行为。
- 影响范围：`internal/cli`、`internal/openplatform`、合同 Skill、README、命令文档与测试计划。
- 关键决策：调用人只取个人 Token；`--mention-id-type` 仅解释被 @ 用户；不发送 `X-MCP-Response-Profile`。
- 关键决策：任务列表按读操作允许一次临时网络重试；评论创建和任务处理禁止自动重试。
- 关键决策：user 下载立即消费 300 秒预签名地址，不输出/记录 URL；校验大小后原子提交。
- 验证结果：`go test ./...`、`go vet ./...`、`go test -race ./...`、`make test`、Linux/Windows amd64 构建与本地代码审查通过。
- 测试修正：移除一个复用同一 `App` 的并行子测试标记，消除存量 `updateNotice` 数据竞争，不改生产逻辑。
- 未处理项或风险项：本期不修改后端、Swagger、飞书接口文档、Higress 配置、版本号或依赖。
